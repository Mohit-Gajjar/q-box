

class PaymentHelper {
  setupPayment() async {
   
  }
  void createPayment(
    String key,
    String txnId,
    String amount,
    String productInfo,
    String firstName,
    String lastName,
    String address,
    String email,
    String phone,
    String surl,
    String furl,
    String hash,
    String service_provider,
  ) async {
    try {
    
    } catch (e) {
      print(e);
    }
  }
}
